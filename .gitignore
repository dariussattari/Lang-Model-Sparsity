# Python venvs (per-model, hundreds of MB to GB each)
*/.venv/
.venv/

# Python caches
__pycache__/
*.pyc
*.pyo

# Model weights (auto-downloaded by runners; not source)
*.pt
*.pth
*.onnx
*.safetensors

# Temp / scratch from probing
frame*.jpg
probe*.py
yolo/detections/

# Regenerable annotated videos (the results JSON/CSV are the real artifacts)
*_annotated.mp4

# pruning experiment: regenerable dataset, source footage, logs, training dumps
pruning/dataset/
pruning/dataset_far/
pruning/seraphim/
pruning/quadcopter.mov
pruning/*.log
pruning/reference_yolov8_pruning.py
pruning/runs/_train/

# OS cruft
.DS_Store
